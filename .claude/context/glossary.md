# Context: Glossary (pointer)

**Authoritative source: `docs/glossary.md`.** Do not fork a duplicate glossary here — this file exists only so `.claude/` has a discoverable link.

Read `docs/glossary.md` for the full term table (project, mode, step, run, step version, source, claim, verdict, gate, scene, Scene JSON, scene_set, layout class, semantic storyboard, classifier, constraint preset, layout_override, motion preset, dirty scene, cache_key, produce, render, format, asset, provider, capability, chain, tier, failover, exhausted, cost cap, publish, AI disclosure, DLQ, correlation_id) and its "Domain rules cần nhớ" section (6 rules most likely to be gotten wrong).

If you learn a new term while working, add it to `docs/glossary.md` directly — that's the one source of truth for terminology, referenced by code, UI, and docs alike.

See [business-domain.md](business-domain.md) for the narrative version of the same concepts.
