# Project Memory

**Last updated:** 2026-07-13 (tasks/ verification pass). Keep this concise — archive obsolete entries rather than letting this file grow unbounded. This is the one file every session should check first (see `CLAUDE.md` §2).

## Current State

Task 1-1 established the monorepo foundation: FastAPI health service, Next.js login placeholder, local Docker development stack, CI/env-template gate, and smoke/unit tests. `docs/` remains the normative BA/architecture handoff; implementation must continue from the task dependency graph, starting with unblocked task 2-1 (Scene JSON schema) and then Epic 1 foundations.

## Open Questions

- 2026-07-17: `sprint-status.yaml`/`RUN-STATUS.md`/`state/*.json` reconciled against actual git history (see RUN-STATUS.md "Audited" line). Tasks **3-3 (LLM adapters), 3-4 (API key management), 3-5 (cost tracking), 5-1 (workspace UI)** have real, substantial implementations sitting only in worker-B's unpushed worktree (`.claude/worktrees/agent-a3b47cc427c4448c4`, branch `worktree-agent-a3b47cc427c4448c4`, 9 bundled commits) — no dedicated `feat/*` branch exists for these 4 tasks yet. Needs worker-B (or whoever picks these tasks up) to split the relevant commits/files out onto proper per-task branches and push before those tasks can be marked done or safely built upon.
- 2026-07-17 (1-3 done): Task 1-3's Step-7 integration/e2e tests (`test_projects_crud.py`, `test_projects_permissions.py`, Playwright `create-project.spec.ts`) were **not** written — this sandbox has no live Postgres or browser. Unit tests (`tests/unit/services/test_project_service.py`, 15 green) cover the business-rule ACs (BR-1/2/3/4/6) directly against the service layer instead. Whoever has a full dev environment should add the integration/e2e layer before considering 1-3 fully DoD-complete in the strict sense.
- 2026-07-17 (1-5 done): Same environment gap — task 1-5's Step-6 property test (random create/restore sequence preserving "current always determinable"/"no version lost" invariants) needs a real DB-backed session to be meaningful and wasn't written. 11 unit tests (`tests/unit/services/test_versioning_service.py`) cover AC 1-5 against `VersioningService` with a FakeSession instead. Also found: `app/api/versions.py` as it existed on `origin/feat/1-5-versioning-engine` called `Depends(get_session)`, a function that doesn't exist anywhere in this codebase (would 500 every request) — rewritten to the `request.app.state.database.session()` pattern used everywhere else. If another task adds a real `get_session` FastAPI dependency later, reconcile rather than leaving two competing session-acquisition patterns.
- 2026-07-17: `tests/unit/test_event_envelope.py` (5 tests) calls `ProjectStateMachine.transition(project_id=..., from_state=..., to_state=..., correlation_id=..., actor=..., reason=...)` — a signature that doesn't match the current `transition(self, project, to_state, actor, reason=None, session=None)` in `app/services/state_machine.py` (task 1-4, already marked done). This is a task-1-4 regression/drift, not 1-5 — versioning_service.py never calls `.transition()`. Left as-is (out of scope for 1-3/1-5/2-5); flagged here so whoever revisits 1-4 doesn't have to rediscover it.
- 2026-07-17: `frontend/src/components/workspace/Topbar.tsx` (task 5-1) has a broken import (`./ui/status-badge` — wrong relative path) and `frontend/src/lib/schema-form/generate.ts` (task 5-6?) contains JSX but has a `.ts` extension instead of `.tsx`, causing a syntax error. Both pre-date this session (introduced by task 5-1/schema-form commits) and currently break `npm run build` for the *entire* frontend, not just those features — discovered while verifying task 1-3's dashboard UI (which itself type-checks and lints clean in isolation via `npx tsc --noEmit` / `npx eslint` scoped to the new files). Whoever owns 5-1/5-6 next should fix both — they're one-line/one-rename fixes, but out of scope for 1-3/1-5/2-5.

## Known Issues

- `docs/specs/remotion-integration.md` has a cosmetic section-numbering gap (§4 was inserted before the old §3, which got renumbered to §5 — no standalone §3 exists). Not user-flagged, not fixed, low priority.
- `.claude/` content was generated from `docs/` in a single bootstrap pass — treat as first draft; cross-check specific numbers against `docs/` before relying on them, per [context/architecture.md](../context/architecture.md) staleness note in CLAUDE.md §11. The 65 `.claude/tasks/*.md` files got a full verification pass on 2026-07-13 (path conventions, dependency chain, cross-links) — those are now more trustworthy than the rest of the untouched bootstrap content.

## Technical Debt

None yet — no code exists to accrue debt in. Watch for: Constraint Resolver v1 uses fixed presets per layout class (not a general solver) — explicitly deferred to v1.1+ per ADR-0008, revisit only if the 11-class catalog becomes a demonstrated bottleneck.

## Lessons Learned (see full detail in postmortems/ and mistakes/)

- A principle stated in one prominent doc location isn't automatically propagated everywhere it's referenced — verify via exhaustive search, not memory (M-001).
- An ambiguous sentence in a spec is often a symptom of an undefined concept — fix the concept, not just the wording (M-002).
- Skill/process guidance needs to be task-level with a verification mechanism (PR DoD tie-in), not vague story-level notes (M-004).
- A coding agent's own dev sandbox (network allowlist, per-command timeout) is a dev-tooling constraint, distinct from the production provider-chain design — don't let sandbox workarounds (chunked rendering, offline TTS binaries) leak into production architecture docs. Captured in [patterns/sandboxed-agent-network-fallback.md](../patterns/sandboxed-agent-network-fallback.md), sourced from a prior end-to-end TikTok-video pipeline built entirely in such a sandbox.

## Future Improvements

- Once Phase 1 code lands: replace all "intent, not yet verified" notes in `.claude/context/build-process.md`, `coding-style.md`, `testing-strategy.md` with real findings.
- Populate `.claude/context/dependencies.md` with real `pyproject.toml`/`package.json` contents once they exist.
- Revisit `.claude/` for duplication/drift after the first few real PRs — this bootstrap was generated without real code to validate against.

## Completed Milestones

- 2026-07-10: `docs/` handoff package complete (SRS v3.0, ARCHITECTURE, CONFIGURATION, full backlog, design system + wireframe).
- 2026-07-11 to 12: Layout Engine architecture pivot (Gamma-style, AI-never-chooses-layout) designed, self-audited, and fully propagated across docs. Remotion runtime-integration deep-dive completed (dev-time skills vs. runtime boundary, Scene/Video composition split).
- 2026-07-12: `.claude/` AI workspace bootstrapped (agents, context, rules, workflows, prompts, templates, checklists, patterns, anti-patterns, decisions, postmortems, reviews, mistakes, memory).
- 2026-07-13: Added [patterns/sandboxed-agent-network-fallback.md](../patterns/sandboxed-agent-network-fallback.md) (offline-binary-first, local-run-script-for-blocked-paid-APIs, chunked-render+concat, measured-duration timeline.json) and linked from `context/build-process.md` + `patterns/provider-adapter.md`. Added `elevenlabs` as a paid TTS provider option in `docs/CONFIGURATION.md` §4/§11.3 (user-requested — ElevenLabs env key).
- 2026-07-13: Improved storytelling prompt chain in `docs/specs/prompts.md` §5-7 (user-requested, "ok apply đi"): shared persona line repeated across all 3 LLM calls (they're independent, possibly different providers in chain — can't rely on implicit context carry-over); `outline.generate` now requires hook to be one of 3 concrete types (shocking stat / counter-intuitive question / cross-source controversy) + new `controversy` field + per-section word budget instead of one total; `script.generate` adds sentence-rhythm variety (mix short/medium, not uniform <20-word) + TTS-unfriendly-character exclusion (emoji, unspellable abbreviations); `storyboard.generate` adds a worked few-shot example of correct `narration_anchor` extraction, a self-check checklist before JSON output, and a no-consecutive-repeat-purpose/component-kind constraint.
- 2026-07-13: Upgraded `.claude/tasks/` for long-running autonomous agent-team execution (user request: "cần chi tiết hơn nữa" — the old Scope/BR/AC/DoD-only shape wasn't enough for smaller models to self-drive without stopping to ask). Added: [tasks/TASK-TEMPLATE.md](../tasks/TASK-TEMPLATE.md) (state-file header, numbered Execution Steps with Files/Do/Verify/On-failure/Commit, mandatory Retrospective, Resuming-after-interruption); [tasks/state/](../tasks/state/) — one `{task-id}.json` resumable-state stub per task (all 65 created, all validated as parseable JSON) plus `state/README.md` (resume algorithm) and `state/RUN-STATUS.md` (rollup); [workflows/autonomous-task-execution.md](../workflows/autonomous-task-execution.md) (claim→branch→execute→retry→commit/push→DoD→retrospective→next-task loop; 3-attempt retry policy distinguishing transient vs. non-transient failures; git automation scope). Per explicit user decision, auto `git checkout -b`/`commit`/`push origin feat/*` are now durably pre-authorized (no per-action confirmation) — scoped to feature branches only via new allow-rules in `.claude/settings.json`; push to `main`, force-push, and PR-opening remain gated. All 65 task files rewritten into the new template shape via 10 parallel background subagents (one per epic); Epic 9's first subagent hit a session-limit error partway through and was recovered via a precisely-scoped follow-up subagent that resumed only the incomplete tasks without redoing finished work. Post-rewrite verification found and fixed a path-convention drift across ~52 of the 65 files (subagents had used bare `app/...`/`tests/...` instead of the authoritative `backend/app/...`/`backend/tests/...`/`frontend/tests/...` per `docs/dev-guide.md` §1) via a two-pass scripted fix plus manual cleanup of stragglers the script's heuristics missed; also found and fixed a stale dependency-chain error in `tasks/README.md`'s Track A summary (see git history for the fix). Two doc-level contradictions surfaced during verification and were logged under Open Questions rather than silently resolved: the `MODE1_AUTOPUBLISH=on` SRS-vs-epic-07 contradiction, and the missing frontend test-directory convention in `docs/dev-guide.md`.
- 2026-07-13: Completed task 1-1. Docker host publishing requires API/frontend to join a non-internal `edge` network as well as the private internal network; services that must remain private stay internal-only. `make up` was verified with `GET /health` returning `{status: ok, db: ok}`; backend tests and lint/type checks pass.
- 2026-07-13: Completed task 2-1. Pydantic is the sole Scene JSON source; `make gen-scene-schema` exports the committed JSON Schema and regenerates the Remotion Zod runtime converter. The initial source-code converter emitted `z.any()` for `$ref`, so the generated module instead uses `zod-from-json-schema`, which preserves referenced nested contracts. Shared fixtures are exercised by both pytest and Vitest.

## Open Questions


- No CI provider chosen yet (GitHub Actions implied by `docs/` structure but not confirmed).
- Actual `Makefile` target names, exact `pyproject.toml`/`package.json` dependency versions — all TODO until Phase 1 scaffolding.
- Remotion company license review (needed before commercialization, per SRS §12 risk table) — not yet done, no urgency until monetization is active.
- **`MODE1_AUTOPUBLISH=on` contradiction (found 2026-07-13, needs PO decision):** `docs/SRS.md` §2 states gate `on` auto-publishes both `PASS` and `WARN` (subject to the 95%/30-day accuracy threshold). `docs/backlog/epic-07-automation.md` BR-3 (line 101) states gate `on` auto-publishes `PASS` only, `WARN` always stops for approval — and explicitly claims this "đúng SRS" (matches SRS), which it does not. Task file `.claude/tasks/7-3-gate-config-thong-ke.md` was written from the epic doc, so it currently encodes the epic's (contradicting) version. Do not implement either behavior as "obviously correct" — flag to PO for a decision, then fix whichever of `docs/SRS.md` / `docs/backlog/epic-07-automation.md` is wrong (and the derived task file) in the same change, per `CLAUDE.md` §6 "docs are not append-only."
- **Frontend test-directory convention undocumented:** `docs/dev-guide.md` §1's authoritative folder tree specifies `backend/tests/{unit,integration,fixtures}/` but never states an equivalent for `frontend/`. During the 2026-07-13 task-file path-convention cleanup (see Completed Milestones), frontend component-test references were normalized to `frontend/tests/unit/...` by inference (mirroring the backend convention), and Playwright e2e-spec paths (`tests/e2e/*.spec.ts`, appearing in Epic 5 task files) were left as-is since neither `docs/dev-guide.md` nor `.claude/context/folder-structure.md` states where e2e specs should live. Needs a PO/dev-guide decision: `frontend/tests/e2e/` vs. a root-level `e2e/` vs. co-located `frontend/tests/{unit,e2e}/`.

## Unknown Areas

Everything about actual runtime behavior, actual performance numbers (render latency, cache hit rates), actual CI/lint configuration, and actual test coverage — none of this can be known until code exists. Do not fill these in with guessed values; mark Unknown/TODO until measured.
